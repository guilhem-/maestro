# ESP32-C3 4MB flash layout for Maestro.
# App ~1.4MB (0x160000), LittleFS ~2.5MB (0x290000 = 2,686,976 bytes).
# Note: the partition Type for LittleFS is still "data" with SubType "spiffs"
# in arduino-esp32 v3 — the filesystem driver is selected at build time via
# `board_build.filesystem = littlefs` in platformio.ini, not by the SubType name.
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x160000,
littlefs, data, spiffs,  0x170000, 0x290000,
